Experiment B uses a fully connected mesh of 8 nodes.
Each node measures distances to 7 other nodes.
When you fuse 7 noisy measurements, the error drops by roughly 1 / sqrt(7) (a factor of ~2.6).
So for Noise = 5m:

Expected Error ≈ 5m / 2.6 ≈ 1.9m.
Observed Error = ~1.82m.